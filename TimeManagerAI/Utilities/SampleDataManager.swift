import Foundation

class SampleDataManager {
    static func createSampleTasks(in context: Any? = nil) {
        print("✅ Sample data creation placeholder - would create sample tasks and events")
    }

    static func shouldCreateSampleData(in context: Any? = nil) -> Bool {
        return false // Disable sample data for now
    }
}